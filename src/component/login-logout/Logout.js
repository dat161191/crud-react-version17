import React from "react";
import { Container } from "react-bootstrap";

const Logout = () => {
    return (
        <Container>
            Logout
        </Container>
    )
};
export default Logout;